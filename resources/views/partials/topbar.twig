<section id="topbar">

    <div class="topbar-left">
        <a href="/admin" class="logo variant-logo">
            {{ img('theme::img/conduct-logotype.svg').data|raw }}
        </a>
    </div>
    <div class="navbar navbar-default">
        <div class="container-fluid">
            <ul class="nav navbar-nav navbar-left">
                <li class="nav-item variant-border {{ active_entry.slug == 'anomaly.module.pages' ? 'active' }}" >
                    <a href="/admin/pages">
                        <i class="icon {{ trans('theme::control_panel.topbar_sections.pages.icon') }}"></i>
                        <span class="title">{{ trans('theme::control_panel.topbar_sections.pages.name') }}</span>
                    </a>
                </li>
{#                <li class="nav-item variant-border {{ active_entry.slug == 'anomaly.module.posts' ? 'active' }}">#}
{#                    <a href="/admin/posts">#}
{#                        <i class="icon {{ trans('theme::control_panel.topbar_sections.posts.icon') }}"></i>#}
{#                        <span class="title">{{ trans('theme::control_panel.topbar_sections.posts.name') }}</span>#}
{#                    </a>#}
{#                </li>#}
                <li class="nav-item variant-border {{ active_entry.slug == 'anomaly.module.files' ? 'active' }}">
                    <a href="/admin/files">
                        <i class="icon {{ trans('theme::control_panel.topbar_sections.files.icon') }}"></i>
                        <span class="title">{{ trans('theme::control_panel.topbar_sections.files.name') }}</span>
                    </a>
                </li>
                <li class="nav-item variant-border {{ active_entry.slug == 'anomaly.module.users' ? 'active' }}">
                    <a href="/admin/users">
                        <i class="icon {{ trans('theme::control_panel.topbar_sections.users.icon') }}"></i>
                        <span class="title">{{ trans('theme::control_panel.topbar_sections.users.name') }}</span>
                    </a>
                </li>
                <li class="nav-item variant-border {{ active_entry.slug == 'dk_behavior.module.ax_shop' ? 'active' }}">
                    <a href="/admin/ax_shop">
                        <i class="icon fas fa-tags"></i>
                        <span class="title">{{ trans('dk_behavior.module.ax_shop::addon.title') }}</span>
                    </a>
                </li>
                <li class="nav-item variant-border dropdown {{ active_entry.slug not in ['dk_behavior.module.ax_shop'] and not (active_entry.slug starts with 'anomaly.module') ? 'active' }}">
                    <a href="#" data-toggle="dropdown">
                        <i class="icon {{ trans('theme::control_panel.topbar_sections.modules.icon') }}"></i>
                        <span class="title">{{ trans('theme::control_panel.topbar_sections.modules.name') }}</span>
                    </a>

                    <ul class="dropdown-menu-left dropdown-menu">
                        {% for key, navigation in template.cp.navigation %}
                            {% if navigation.slug not in ['dk_behavior.module.ax_shop'] and not (navigation.slug starts with 'anomaly.module') %}
                                <li class="dropdown-item">
                                    <a {{ html_attributes(navigation.attributes) }}>
                                        {{ navigation.icon()|raw }}
                                        {{ trans(navigation.title) }}
                                    </a>
                                </li>
                            {% endif %}
                        {% endfor %}
                    </ul>
                </li>
                <li class="nav-item variant-border dropdown  {{ active_entry.slug not in ['anomaly.module.dashboard','anomaly.module.pages','anomaly.module.posts','anomaly.module.files','anomaly.module.users'] and active_entry.slug starts with 'anomaly.module' ? 'active' }}">
                    <a href="#" data-toggle="dropdown">
                        <i class="icon {{ trans('theme::control_panel.topbar_sections.settings.icon') }}"></i>
                        <span class="title">{{ trans('theme::control_panel.topbar_sections.settings.name') }}</span>
                    </a>

                    <ul class="dropdown-menu-left dropdown-menu">
                        {% for key, navigation in template.cp.navigation %}
                            {% if navigation.slug not in ['anomaly.module.dashboard','anomaly.module.users'] and navigation.slug starts with 'anomaly.module' %}
                                <li class="dropdown-item">
                                    <a {{ html_attributes(navigation.attributes) }}>
                                        {{ navigation.icon()|raw }}
                                        <span class="title">{{ trans(navigation.title) }}</span>
                                    </a>
                                </li>
                            {% endif %}
                        {% endfor %}
                    </ul>
                </li>
            </ul>

            {#            <div class="button-menu"> #}
            {#                            {{ breadcrumb() }} #}
            {#            </div> #}


            {# <div class="button-menu"> #}
            {# <button href="javascript:void(0);" type="button" class="button-menu-mobile" #}
            {# onclick="$('#sidebar').toggleClass('open'); $('body').toggleClass('expand');"> #}
            {# <i class="fa fa-bars"></i> #}
            {# </button> #}
            {# </div> #}

            {# <form class="navbar-form dropdown" id="search"> #}

            {# <input type="text" class="form-control search-bar" placeholder="Type to search..."> #}

            {# <button type="submit" class="btn btn-search"><i class="fa fa-search"></i></button> #}

            {# <div class="dropdown-menu results"> #}
            {# <h6 class="dropdown-header">Navigation</h6> #}
            {# {% for key, navigation in template.cp.navigation %} #}
            {# <a class="dropdown-item" {{ html_attributes(navigation.attributes) }}> #}
            {# <span class="title">{{ trans(navigation.title) }}</span> #}
            {# </a> #}
            {# {% endfor %} #}
            {# <h6 class="dropdown-header">Links</h6> #}
            {# <a href="/" target="_blank" class="dropdown-item"> #}
            {# {{ trans('theme::control_panel.view_site') }} #}
            {# </a> #}
            {# <a href="/admin/logout" class="dropdown-item"> #}
            {# {{ trans('theme::control_panel.logout') }} #}
            {# </a> #}
            {# </div> #}

            {# </form> #}

            <ul class="nav navbar-nav navbar-right">
                {% for shortcut in template.cp.shortcuts %}
                    <li class="nav-item shortcut {# dropdown #}">
                        <a {{ html_attributes(shortcut.attributes) }} {# data-toggle="dropdown" #}>
                            {# <span class="tag tag-danger">{{ icon(shortcut.icon) }} 2</span> #}
                            {{ icon(shortcut.icon) }}
                        </a>
                        {# <ul class="dropdown-menu-right dropdown-menu"> #}
                        {# <li class="dropdown-item"> #}
                        {# <a href="/" target="_blank"> #}
                        {# <i class="fa fa-external-link"></i> #}
                        {# {{ trans('theme::control_panel.view_site') }} #}
                        {# </a> #}
                        {# </li> #}
                        {# <li class="dropdown-item"> #}
                        {# <a href="/admin/logout"> #}
                        {# <i class="fa fa-power-off"></i> #}
                        {# {{ trans('theme::control_panel.logout') }} #}
                        {# </a> #}
                        {# </li> #}
                        {# </ul> #}
                    </li>
                {% endfor %}

{#                {% if template.module.composer_json.support %}#}
{#                    #}{#                    <li class="nav-item shortcut dropdown"> #}
{#                    <li class="nav-item dropdown">#}
{#                        <a href="#" data-toggle="dropdown">#}
{#                            #}{#                            {{ icon('fa fa-life-ring') }} #}
{#                            <i class="icon far fa-life-ring"></i>#}
{#                            <span class="title">{{ trans('theme::control_panel.support') }}</span>#}
{#                        </a>#}

{#                        <ul class="dropdown-menu-right dropdown-menu">#}
{#                            <li class="dropdown-header">#}
{#                                {{ trans('streams::support.support', {'type': trans(template.module.title)}) }}#}
{#                            </li>#}
{#                            <li class="dropdown-divider"></li>#}
{#                            {% for type, value in template.module.composer_json.support %}#}
{#                                {% if type == 'email' %}#}
{#                                    <li class="dropdown-item">#}
{#                                        <a href="mailto:{{ value }}">#}
{#                                            {{ value|split('?')[0] }}#}
{#                                        </a>#}
{#                                    </li>#}
{#                                {% else %}#}
{#                                    <li class="dropdown-item">#}
{#                                        <a href="{{ value }}" target="_blank">#}
{#                                            {{ trans('streams::support.' ~ type) }}#}
{#                                        </a>#}
{#                                    </li>#}
{#                                {% endif %}#}
{#                            {% endfor %}#}
{#                            #}{# <li class="dropdown-divider"></li> #}
{#                            #}{# <li class="dropdown-item"> #}
{#                            #}{# <a href="#" data-toggle="modal" data-target="#modal-help"> #}
{#                            #}{# {{ trans('streams::support.more') }} #}
{#                            #}{# </a> #}
{#                            #}{# </li> #}
{#                        </ul>#}
{#                    </li>#}
{#                {% endif %}#}
                <li class="nav-item">
                    <a href="/" target="_blank">
                        <i class="fas fa-external-link-alt"></i>
                        {{ trans('theme::control_panel.view_site') }}
                    </a>
                </li>
                <li class="nav-item dropdown">
                    <a href="javascript:void(0);" data-toggle="dropdown">
                        {% set nameSplit = user().display_name|split(' ') %}
                        {% set initials = '' %}
                        {% for part in nameSplit %}
                            {% set initials = initials ~ part|first|upper %}
                        {% endfor %}
                        {% set gravatar = user().gravatar({'d':'404'}) %} {# identicon{{ user().display_name }}&nbsp; #}
                        {#                        {{ dd(gravatar) }} #}
                        <div class="user-image">
                            <span class="initials">{{ initials }}</span>
                            <img src="{{ gravatar.path }}" width="40"
                                 class="rounded-circle" onerror="this.style.opacity='0'">
                        </div>
                    </a>
                    <ul class="dropdown-menu-right dropdown-menu">
                        <li class="dropdown-item">
                            <a href="/admin/users/edit/{{ user().id }}">
                                <i class="icon fa fa-user-alt"></i>
                                <span class="title">{{ trans('theme::control_panel.profile') }}</span>
                            </a>
                        </li>
                        <li class="dropdown-item">
                            <a href="/admin/logout">
                                <i class="icon fas fa-sign-out-alt"></i>
                                <span class="title">{{ trans('theme::control_panel.logout') }}</span>
                            </a>
                        </li>
                    </ul>
                </li>
            </ul>

        </div>
    </div>
</section>
